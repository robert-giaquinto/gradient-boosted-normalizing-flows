# Settings shared across all models and experiments
early_stop=50
epochs=500
warmup_epochs=0
learning_rate=0.001
lr_schedule=cosine
batch_size=64
epochs_per_component=500
max_grad_norm=5.0
weight_decay=0.0
coupling_network_depth=1
